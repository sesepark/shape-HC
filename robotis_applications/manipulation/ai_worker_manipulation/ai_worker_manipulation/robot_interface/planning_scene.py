# Manages the MoveIt2 planning scene — the world model used for collision checking.
# Registers fixed obstacles (table, tray, peg board) once at startup.
# Attaches and detaches objects when the gripper picks and places them.
